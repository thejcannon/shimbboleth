pre-commit:
  parallel: true
  commands:
    ruff-format:
      glob: "*.py"
      run: uv run ruff format {staged_files}
    ruff-fix:
      glob: "*.py"
      run: uv run ruff check --fix {staged_files}
    pyright:
      run: uv run pyright
